# Obsidian Chat Memory — dependencies (lean stack, no PyTorch)
# Python 3.10+ required.
# Embeddings via fastembed (ONNX, ~50 MB) instead of sentence-transformers (~2 GB).

fastapi>=0.136
uvicorn[standard]>=0.48
pydantic>=2.13
chromadb>=1.5
scikit-learn>=1.5
numpy>=2.0
fastembed>=0.8
google-genai>=2.7
python-dotenv>=1.0

# Optional: MCP server (for Claude Desktop)
mcp>=1.27
